# Run from F:\Tracking in PowerShell.
#
# Full final render for every protected source video. The pipeline uses
# LocateAnything 3B first, then Qwen3-VL-4B, both quantized to 8-bit.
.\scripts\run_all_final_videos.ps1 -Resume -Overwrite
