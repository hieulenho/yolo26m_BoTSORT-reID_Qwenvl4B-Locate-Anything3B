# Benchmark Tools

Builders for detector, tracker, IDSW, semantic, runtime, and final reports. Their inputs are
versioned contracts under `configs/benchmarks/`; generated results belong under
`outputs/benchmarks/`.
