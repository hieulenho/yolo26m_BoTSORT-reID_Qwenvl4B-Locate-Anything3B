# Legacy Scripts

Compatibility entry points for earlier football-only A/B/C experiments. They are retained for
reproducing historical reports, but new experiments should use
`scripts/run_adaptive_tracking.ps1` or `scripts/run_realtime_adaptive.ps1`.
