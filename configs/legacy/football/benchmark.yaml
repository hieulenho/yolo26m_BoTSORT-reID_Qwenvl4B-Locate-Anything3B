inputs:
  detector_metrics: outputs/metrics/yolov8m_finetuned_val.json
  tracker_overall: outputs/metrics/experiments/tracker_summary.json
  tracker_per_sequence: outputs/metrics/experiments/tracker_sequence_metrics.csv

output:
  root: outputs/metrics/benchmark
  figures_root: outputs/figures/benchmark

runtime:
  overwrite: true
  log_level: INFO
